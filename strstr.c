#include "main.h"

/**
 * _strstr - Search a string in a string
 * @haystack: The string to search inside
 * @needle: The string to search
 * Return: char
*/
char *_strstr(char *haystack, char *needle)
{
	if (_strlen(needle) == 0)
		return (haystack);
	
}
